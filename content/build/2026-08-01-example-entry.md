---
title: "Example entry — delete me"
date: "2026-08-01"
type: "general"
---

This is a placeholder Build entry so you can see the frontmatter format in action. Delete this file once you've written a real one.

Frontmatter fields:

- `title` — shows as the entry title
- `date` — used for sorting, newest first
- `type` — `general` or `click` (controls the pink "// THE CLICK" treatment)

Everything below the second `---` is regular markdown — headings, lists, links, bold/italic all work.
